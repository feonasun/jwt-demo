server:
  port: 8080

spring:
  docker:
    compose:
      enabled: false  # 禁用 Docker Compose 自动配置
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/jwtdemo?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: jwt_dev
    password: devuser123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update  # ??????validate
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true

jwt:
  secret: mySecretKeyMySecretKeyMySecretKeyMySecretKey  # ???32??
  expiration: 86400000  # 24??????